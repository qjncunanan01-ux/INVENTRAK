// Expo entry point — registers the root React component so the app can boot
// under `npx expo start` (Expo Go / dev client / production builds).
import { registerRootComponent } from 'expo';
import App from './src/App';

registerRootComponent(App);
