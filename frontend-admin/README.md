# INVENTRAK Admin Web

This is the admin web application for INVENTRAK. It includes inventory, product, and optimization pages.

Run locally:

```bash
cd frontend-admin
npm install
npm start
```

The app expects the backend API at `http://localhost:4001`.
